{% extends "base.html" %}
{% block title %}MyMusic ­‐ {{ song.name }}{%	endblock %}
{% block content %}

<span vocab="http://schema.org/" typeof="Restaurant">	
	<h1>	
		<span property="name">{{ song.name }}</span>
		{% if user == song.user %}
			(<a	href="{% url 'myrestaurants:restaurant_edit' restaurant.id %}">edit</a>)	
  		{% endif %}
  	</h1>	

<h1>
    {{ song.name }}
    {% if user == song.user %}
    (<a href="{% url 'imusica:song_edit' song.artista.id song.id %}">edit</a>)
    {% endif %}
</h1>
<p>{{ song.artista}}, {{ song.album}}</p>
<p>{{ song.style }}</p>
<p>{{ song.texte }}</p>



{% endblock %}

{% block footer %}
    Created by {{ song.user }} on {{ song.date }}
{% endblock %}
